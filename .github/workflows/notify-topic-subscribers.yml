name: Notify topic subscribers (disabled)

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy topic notifications are disabled
        run: |
          echo "Legacy topic Telegram notifications are disabled."
